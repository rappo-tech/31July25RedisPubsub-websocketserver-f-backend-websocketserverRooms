page.tsx==>create.tsx
websocketserver.ts
route.ts==>sendWebsocket.ts

#room websocketServer.ts 
1. connection open,receive msg, broadcastmsg to all client
2. store all  the clinets  concted to the port in  new Set()
3. declare  make all the  room  
4. receive msg  on port8080 but alogside msg also recieve room and msg ,  for certain room(x ) only 
5. broadcast the msg  only on certain room(x) 
socket.send(msg,room)

#page.tsx
1.connect to socket,reveive msg from websoxketserver,send msg to websocket server 
2.while sending msg also send which room and msg (ws.send())
3.and while receiving msg also show msg  and for which room 
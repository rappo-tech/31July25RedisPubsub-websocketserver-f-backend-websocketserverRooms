generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model AllTweets{
id String @id  @default(uuid())
tweetName String
like  Int 
}
